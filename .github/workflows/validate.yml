name: Validate and build modpack

on:
  push:
    paths:
      - "packwiz/**"
      - "scripts/**"
      - ".github/workflows/validate.yml"
  pull_request:
    paths:
      - "packwiz/**"
      - "scripts/**"
      - ".github/workflows/validate.yml"

jobs:
  discover:
    runs-on: windows-latest
    outputs:
      versions: ${{ steps.discover.outputs.versions }}
    steps:
      - uses: actions/checkout@v4

      - id: discover
        name: Discover packwiz versions
        shell: pwsh
        run: |
          $versions = ./scripts/discover-versions.ps1
          "versions=$versions" >> $Env:GITHUB_OUTPUT

  validate-and-build:
    needs: discover
    strategy:
      fail-fast: false
      matrix:
        version: ${{ fromJson(needs.discover.outputs.versions) }}
    runs-on: windows-latest
    steps:
      - name: Preserve LF line endings for packwiz hashes
        shell: pwsh
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf

      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Install packwiz
        shell: pwsh
        run: |
          go install github.com/packwiz/packwiz@latest
          Copy-Item (Join-Path (go env GOPATH) 'bin/packwiz.exe') ./packwiz.exe

      - name: Validate (${{ matrix.version }})
        shell: pwsh
        run: ./scripts/validate.ps1 -MinecraftVersion ${{ matrix.version }}

      - name: Verify generated index is committed (${{ matrix.version }})
        shell: pwsh
        run: git diff --exit-code -- packwiz/${{ matrix.version }}/pack.toml packwiz/${{ matrix.version }}/index.toml

      - name: Build Modrinth package (${{ matrix.version }})
        shell: pwsh
        run: ./scripts/pack.ps1 build -Version ${{ matrix.version }}

      - name: Archive Modrinth package (${{ matrix.version }})
        shell: pwsh
        run: |
          $mrpacks = @(Get-ChildItem ./dist -File -Filter '*.mrpack')
          if ($mrpacks.Count -ne 1) {
            throw "Expected one .mrpack for Minecraft ${{ matrix.version }}, found $($mrpacks.Count)."
          }

          $archiveDirectory = Join-Path 'artifacts' '${{ matrix.version }}'
          $archivePath = Join-Path $archiveDirectory "Preliminary-Art-Form-${{ matrix.version }}.zip"
          New-Item -ItemType Directory -Force -Path $archiveDirectory | Out-Null
          Compress-Archive -LiteralPath $mrpacks[0].FullName -DestinationPath $archivePath -Force

      - name: Upload package (${{ matrix.version }})
        uses: actions/upload-artifact@v4
        with:
          name: Preliminary-Art-Form-${{ matrix.version }}
          path: artifacts/${{ matrix.version }}/Preliminary-Art-Form-${{ matrix.version }}.zip
          if-no-files-found: error
